import { styled } from 'solid-styled-components'

export const SiteLayout = styled('section')`
  display: grid;
  grid-template-rows: auto;
  place-content: center;
  width: fit-content;
`

export const Name = styled('span')`
  text-align: center;
  margin: auto;
`
